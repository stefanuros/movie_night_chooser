// This file handles the apps style
import 'package:flutter/material.dart';

abstract class Style {
	// static const accentColour = Colors.teal;
	static const accentColour = Colors.red;
	// static final splashColour = Colors.teal[300];
	static final splashColour = Colors.red[300];
}
